# `/horsewhip-lock`

**Active.** Read `.cursor/skills/horsewhip-lock/SKILL.md` and follow the MCP order strictly.

## Hard rule

**Add new files only — do not modify any file already tracked by git (including meta files).** To change existing files, use `expand_boundary` + user approval.

## Order

1. `horsewhip_lock_decouple` + `userText`
2. `horsewhip_whip_ceremony` lock
3. Create new files only + `record_write`
4. `task_complete`

Task = text after `/horsewhip-lock`.
