# Chitu

> 马鞭策 chitu。人中吕布，马中赤兔。
>
> 架构不是设计出来的，是锁锁出来的。

---

## 它是什么

一个终端里跑的 AI Agent。不依赖 Claude Code、不依赖 Cursor、不依赖任何现成的 Agent SDK。自己调 DeepSeek V4 Pro API，自己管工具调用，自己读文件写文件。

它唯一依赖的外部东西是 Horsewhip——马鞭帮它守门，告诉它什么文件能碰什么不能碰。

---

## 工作方式

人给它一个任务，它进入 **grow 相**。Horsewhip 开 append-only 全锁——所有已有代码只读，AI 只能新建文件。它写新文件，调测试，直到完成。

如果没达标——测试挂了，或者人说不满意——它切到 **trim 相**。Horsewhip 开 lock_intent，圈定刚才新写的那些文件路径作为核心区（strict），同时把注册入口、配置文件、package.json 这类每次都需要的耦合点划为边缘区（warn）。核心区拦截确认，**边缘区自动放行不询问**。现在它可以改这些文件了——修 bug、删冗余、重构。改完了，测试全绿了，锁回去。

两个相互相交替，同一时刻只有一种锁。不需要人手动切锁模式，Chitu 自己判断、自己调 Horsewhip。

---

## 它有哪些能力

**16 个内置工具**，不需要外部 MCP：

读文件、列目录、搜索代码、看 git 状态/差异/日志。这些不守门——想看什么都行。

写文件、编辑文件、删文件、执行 shell 命令。这些守门——每次都调 Horsewhip 问"能不能碰这个文件"。append-only 模式下，新建文件随便，改老文件拦截。trim 模式下，圈内能改，圈外拦住。

网页搜索、网页抓取。只读，不守门。

任务管理（创建任务、更新进度）、问用户（关键决策不能自己做）、跨会话记忆（记住用户偏好）。

**外挂 MCP 和 Skill**：

能加载任意 MCP server（通过 stdio 协议），把外部 MCP 的工具注入自己的工具表。预装 10 个 Skill：code-review、security-review、verify、run、init、claude-api、release-notes、explain-code、refactor，加上 Horsewhip 自己的边界工作流。

---

## 它怎么跑

`chitu run --task "给 server 加 search API"`

人在终端里打字，Chitu 发到 DeepSeek V4 Pro API或者其他大模型（自适配），拿到回复，解析 tool call，执行工具，工具结果塞回去，循环。每一步都在 Horsewhip 的守门下。改不动的东西它改不动，不需要人拦。

上下文太长了自己压缩。人能随时 `Ctrl-C` 暂停，`chitu resume` 恢复。

跑完后自动出六维报告——新增率、召回率、锁回率、回合数、耦合合理度、复用率。

---

## 用 Horsewhip 写 Chitu

Chitu 自身的开发也用 Horsewhip。全锁已有代码，让 AI 在 append-only 下自己决定文件怎么组织。

不预设目录结构。不预设接口签名。只给 AI 上面的自然语言描述。

**验收**：当写一个新 MCP provider 或新增一个 tool 时，不需要解锁任何已有文件。如果必须解锁，说明那个被锁的文件设计不够通用——锁在告诉你：重来。

---

## API 和模型

底层调 DeepSeek V4 Pro API或者其他AI模型（自适配）。streaming 模式，tool-use 解析自己处理。不经过任何中间 SDK——直连 HTTP。

未来可加其他 provider，但这件事不改 core——新 provider 应该作为一个新文件加入，不碰老代码。如果碰了，core 设计有问题。

---

## 技术栈

TypeScript，Node.js，ESM。Horsewhip MCP 通过 stdio 协议连接。API 直连 HTTP。会话存在 `.chitu/sessions/`，纯 JSON 文件。

---

## 硬验收：六维指标

以下六条不是建议，是上线条件。`chitu metrics` 命令必须能跑，每次任务完成后自动输出。数据全从 Horsewhip 写的 `session-audit.json` 里取。

**第一，新增率。** 算新建文件占所有文件写入的比例。高于七成说明大部分工作靠增量完成，架构支持好。低说明老代码老被解锁——耦合太多。

**第二，召回率。** 看同一个文件在同一个任务里被反复解锁改了多少次。多了说明 AI 一次写不对，生成质量差。

**第三，锁回率。** 解锁了几次，就锁回了几次。少一次都不行——解锁了没锁回是流程事故。

**第四，回合数。** 单次任务里解锁、扩界、拦截的总次数。超过五次说明 AI 频繁碰壁，要么边界不清，要么需求飘了。

**第五，耦合合理度。** 看每次解锁的理由是什么。"注册路由"是合理的，"加依赖"是合理的。"直接改比较快"不是——这种偷懒理由占比不能超过一成。

**第六，复用率。** 新写的文件有没有引用已有模块。只有新增不引用老接口，说明在重复造轮子。

不达标就切 trim 修，修到达标为止。没有指标的锁是盲人摸象，没有锁的指标是纸上谈兵。
