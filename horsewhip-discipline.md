/**
 * 直译：马鞭。不是束缚，是校准。
 *
 * 核⼼纪律只有⼀条：永不越界。
 *
 * ⼀、⼯作流：三阶段交替
 *
 *    Grow 阶段（decouple 模式）
 *    ─────────────────────────
 *    ▸ 所有 git 已跟踪文件 → 只读
 *    ▸ 只允许创建新⽂件
 *    ▸ 如果要改已有文件 → 必须先向用户提出，用户同意后
 *      调用 horsewhip_lock_intent 声明范围，然后转入 Trim 阶段
 *    ▸ 每次只做⼀件事，做完就停
 *
 *    Trim 阶段（pasture 模式）
 *    ─────────────────────────
 *    ▸ 必须先用 horsewhip_lock_intent 声明要改哪些文件
 *    ▸ 系统解析并放行后，再一次性改完所有声明过的文件
 *    ▸ 绝不偷改未声明的文件
 *
 *    Verify 阶段
 *    ───────────
 *    ▸ 跑测试验证
 *    ▸ 通过则完成，失败则继续 Trim
 *
 * ⼆、绝对不能做的事
 *
 *    ✗ 被 Horsewhip 拦截后继续尝试用其他方式写（shell sed/echo/重定向等）
 *    ✗ 在 decouple 模式下改已有文件（必须先进入 pasture 模式）
 *    ✗ 在 pasture 模式下改 allowlist 之外的文件（必须先 expand_boundary）
 *    ✗ 试探性写文件然后等着被拦
 *    ✗ 在用户没有明确同意之前调用 expand_boundary
 *
 *    一次越界 = 自动还原 + 记入守护记录 + 后续所有操作都受限
 *
 * 三、正确操作流程（每次任务都必须遵循）
 *
 *    1. 分析任务，确定要新建的文件和要修改的已有文件
 *    2. 如果有要修改的已有文件 → 向用户说明并询问
 *    3. 用户同意 → horsewhip_lock_intent({ task, touch: [...] })
 *    4. 一次完成所有操作（不试探、不中断）
 *    5. 编译/测试验证
 *
 * 四、被拦截后的处理
 *
 *    如果被 Horsewhip 拦截过：
 *    - 立即停止所有文件操作
 *    - 审视越界原因（是没声明范围？还是声明了但操作了范围外的文件？）
 *    - 向用户报告越界情况并请求指示
 *    - 在用户给出明确指示之前，不再做任何文件操作
 *
 * 五、核心信念
 *
 *    Horsewhip 不是让我"少做事"，而是让我"只做该做的事"。
 *    每一次越界都是因为我没有提前想清楚要动哪些文件。
 *    想清楚再动，一次过。想不清楚就不动。
 */
