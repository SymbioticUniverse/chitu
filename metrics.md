# Horsewhip 六维质量报告

> 以下指标全部从 `.git/horsewhip/session-audit.json` 的因果链自动提取。
> 不做判断，只做观察。

## 数据基础

每条审计事件都有：

```json
{ "id": "e-5", "type": "file_unlocked", "file": "src/router.ts",
  "reason": "需注册新路由", "causedBy": "e-2" }
```

`reason` 是 AI 或用户写的自然语言，是分类的基础。

---

## 1. 新增率

**测什么**：多大比例的工作是靠增量完成的，而不是改存量。

**算法**：
```
新增率 = count(write(新文件)) / (count(write(新文件)) + count(write(解锁文件)))
```

- 新文件 = 不在 git tree 中的路径
- 解锁文件 = 在 git tree 中，且本次 task 内出现过 `file_unlocked`
- 取值范围 [0, 1]，越接近 1 越偏增量开发

**怎么看**：
- > 0.8：增量主导，架构支持良好
- 0.4–0.8：混合模式，合理
- < 0.4：存量改动为主，检查是否有大量重复解锁

---

## 2. 召回率

**测什么**：AI 生成代码的一次通过率。

**算法**：
```
召回事件 = 同一 task 内，同一 file 被 file_unlocked 的原因包含
            "bug" / "修复" / "遗漏" / "之前" / "fix" / "补" / "忘"
召回率 = count(召回事件) / count(所有 file_unlocked)
```

**怎么看**：
- 0：AI 一次写对，不需回头改
- < 0.2：可接受，个别遗漏
- > 0.4：该 task 中 AI 反复出错，代码质量低

**注意**：区分召回型解锁和需求变更型解锁。"少了个参数"是召回，"新需求要求加字段"不是。

---

## 3. 锁回率

**测什么**：流程纪律——解锁后有没有锁回去。

**算法**：
```
锁回率 = count(file_locked) / count(file_unlocked)
```

**怎么看**：
- = 1.0：每次解锁都锁回了，纪律满分
- 0.8–0.99：个别遗漏，提醒下
- < 0.8：存在安全隐患，解锁完忘记锁回

---

## 4. 回合数

**测什么**：人机交互的沟通成本。

**算法**：
```
回合 = task_complete 之前所有 file_unlocked + user_expand + strict_block 的总数
单 task 回合数 = 该 task 的回合事件数
```

**怎么看**：
- 1–3：高效，AI 意图清晰
- 4–7：有摩擦，但可接受
- ≥ 8：AI 频繁碰壁或用户频繁调整范围

---

## 5. 耦合合理度

**测什么**：解锁决策是否理性，而非偷懒。

**算法**：对每个 `file_unlocked` 事件，分类其 reason：

| 分类 | reason 匹配 | 判定 |
|------|------------|------|
| 合理耦合 | "注册"/"挂载"/"入口"/"路由"/"配置"/"依赖" | 这些点是天然的耦合点 |
| 接口缺陷 | "接口"/"签名"/"缺少参数"/"参数"/"类型不匹配" | 当初设计时接口考虑不周 |
| 重构优化 | "重构"/"拆分"/"解耦"/"重命名"/"抽象" | 主动改善架构 |
| 需求变更 | "新需求"/"需求"/"追加"/"扩展" | 需求本身变了 |
| 偷懒 | "直接改"/"比较快"/"方便"/"临时"/"先这样" | 放弃思考 |

```
耦合合理度 = count(合理 + 接口缺陷 + 重构) / count(全部解锁)
```

**怎么看**：不看分数看分布。如果"偷懒"占比 > 10%，是流程问题；"接口缺陷"占比高，说明第一批 core 设计不够通用。

---

## 6. 复用率

**测什么**：模块的通用性——被多少地方依赖。

**算法**：跨 task 统计。
```
模块被引用次数 = 不同 task 中对该模块的 file_unlocked 次数
                  + 新建文件中的 import/require 引用该模块的次数

复用率 = 本次 task 中引用了已有模块的 write 数 / count(write(新文件))
```

**怎么看**：
- 高复用率 + 低锁定率：模块接口稳定，所有人都能用，不需要改
- 高复用率 + 高锁定率：大家都在用但不满意，接口该重构
- 低复用率 + 新建文件多：可能在做重复造轮子

**冗余检测**：同一 task 内出现 `write(FooBar.ts)` 和 `write(FooHelper.ts)` 内容相似且功能重叠 → 标记为疑似冗余。

---

## 报告示例

```
╔══════════════════════════════════════════╗
║  Horsewhip 六维报告 · Task: health-check ║
╠══════════════════════════════════════════╣
║ 新增率      ████████░░  0.50            ║
║ 召回率      ██░░░░░░░░  0.00 (0/1)     ║
║ 锁回率      ██████████  1.00            ║
║ 回合数      ██░░░░░░░░  2               ║
║ 耦合合理度  ██████████  1.00 (合理耦合) ║
║ 复用率      ██████░░░░  0.50            ║
╠══════════════════════════════════════════╣
║ 解锁明细：                               ║
║  index.ts → 需注册新路由 [合理耦合]     ║
║                                          ║
║ 冗余标记：无                              ║
║ 偷懒标记：无                              ║
╚══════════════════════════════════════════╝
```

---

## 实现要点

1. **event 解析**：从 session-audit.json 读取 events 数组，按 task 分组
2. **新文件识别**：`write` 事件的 `file` 不在 `git ls-files` 中
3. **reason 分类**：先用关键词规则匹配，再用 LLM 兜底处理模糊 case
4. **跨 task 复用**：需要读 import 图和/或回溯历史 audit 文件
5. **输出**：`horsewhip_get_boundary` 末尾附带指标摘要，或独立 `horsewhip_stats` 工具

**核心原则**：不 judge，只 observe。AI 既偷懒又写 bug，报告会诚实反映出来——这是给人看的镜子，不是给老板的 KPI。
